# haskell-monolish
## Build Docker Image

For stack docker integration.

```sh
$ docker build -t haskell-monolish .
```

## Running Example

```sh
$ # Show the list of examples
$ stack run

$ # Run an example
$ stack run -- xor
```